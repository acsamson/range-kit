/**
 * Vue Components 入口文件
 * 统一导出所有开箱即用的 Vue 组件
 */

export { SelectionPopover } from './SelectionPopover'

// 导出样式路径常量，供用户手动引入
export const SELECTION_POPOVER_STYLE_PATH = 'range-kit-vue/dist/selection-popover.css'
