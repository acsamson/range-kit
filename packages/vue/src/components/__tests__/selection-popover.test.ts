import { describe, it, expect } from 'vitest'
import { SelectionPopover } from '../SelectionPopover'

describe('vue components/selection-popover per-folder tests', () => {
  it('exports a component', () => {
    expect(SelectionPopover).toBeTruthy()
  })
})

