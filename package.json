{
  "name": "range-kit",
  "private": true,
  "version": "0.0.1",
  "description": "Range Kit Monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
