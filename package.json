{
  "name": "range-kit-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Range Kit Monorepo",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check",
    "validate": "pnpm type-check && pnpm -r --filter \"./packages/*\" test",
    "analyze": "node scripts/analyze-bundles.mjs",
    "release": "node scripts/release.mjs",
    "deprecate-all": "node scripts/deprecate.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "typescript": "^5.7.2"
  }
}