# 任务 011: 确保 Core 单测全部通过

## 优先级
🔴 紧急 (Critical)

## 描述
在每个重构任务完成后，必须确保 core 包的所有单元测试通过。这是代码质量的底线。

## 背景
重构过程中可能会破坏现有功能，单测是防止回归的最后一道防线。

## 执行步骤

### 11.1 运行现有测试
```bash
cd core && pnpm test
```

### 11.2 修复失败的测试
- [ ] 分析每个失败的测试用例
- [ ] 确定是测试本身过时还是代码回归
- [ ] 修复代码或更新测试

### 11.3 补充缺失的测试
针对重构后的模块补充测试：

- [ ] **Locator 模块测试**
  - `serializer.test.ts` - 序列化各种选区类型
  - `restorer.test.ts` - 四层恢复算法
  - 边界情况：空选区、跨元素、特殊字符

- [ ] **Highlighter 模块测试**
  - `highlighter.test.ts` - 高亮应用和清除
  - 多种高亮样式测试
  - 重叠高亮处理

- [ ] **错误处理测试**
  - 容器不存在时的错误
  - 无效选区数据的错误
  - 恢复失败的错误

### 11.4 设置 CI 检查
- [ ] 确保 `pnpm test` 在 CI 中运行
- [ ] 设置测试覆盖率阈值（建议 80%）

## 测试命令

```bash
# 运行所有测试
pnpm test

# 运行测试并查看覆盖率
pnpm test -- --coverage

# 运行特定文件的测试
pnpm test -- serializer.test.ts

# 监听模式
pnpm test -- --watch
```

## 验收标准
- `pnpm test` 全部通过（0 failures）
- 核心恢复算法测试覆盖率 > 90%
- 每个公共 API 方法至少有一个测试用例

## 检查点
在以下任务完成后必须运行测试：
- ✅ 001 完成后
- ✅ 002 完成后
- ✅ 003 完成后
- ✅ 004 完成后
- ✅ 005 完成后
- ✅ 006 完成后
- ✅ 009 完成后

## 预估工作量
持续进行，每个任务后约 15-30 分钟

## 相关文件
- `core/src/**/__test__/`
- `core/vitest.config.ts`
- `core/package.json` (test script)
